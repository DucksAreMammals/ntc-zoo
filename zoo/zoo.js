let elephantCount = 2;
let tigerCount = 4;
let pandaCount = 1;

let animalCount = elephantCount + tigerCount + pandaCount;
console.log(`There are ${animalCount} animals in the NTC Zoo.`);

let isZooOpen = true;
let zooStatus = isZooOpen ? "open" : "closed";
console.log(`The zoo is ${zooStatus}!`);
